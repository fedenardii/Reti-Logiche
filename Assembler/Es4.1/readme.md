# Esercitazione 4.1
## Esame 2023-01-10

## Esercizio Assembler

Scrivere un programma Assembler che si comporta come segue:

* Legge e fa eco, su due righe distinte, due naturali *x* e *y* in base 10 su 16 bit, usando il sottoprogramma `indecimal`.
* Calcola, utilizzando l'istruzione MUL a 8 bit, il prodotto *z = x · y*.
* Stampa su una nuova riga il risultato *z*.
* Termina.

### Note:

* L'istruzione MUL va utilizzata solo nella versione a 8 bit.
* Per ogni istruzione che non sia la MUL, non vi è alcun vincolo sul numero di bit.
* Per la lettura e stampa di numeri decimali è sufficiente la validazione fatta da `indecimal` e `outdecimal`.

---

Un esempio di output è allegato in formato .txt al link: https://tinyurl.com/2kmhxmev  
